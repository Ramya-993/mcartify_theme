import ProductDescServer from "./ProductDescServer";

// Main ProductDesc component that handles server-side rendering for SEO
// This is now a server component that fetches data server-side and passes it to the client component
export default ProductDescServer;
